/*
 * Date: 2018-
 *
 * Description:
 * https://www.geeksforgeeks.org/find-minimum-time-to-finish-all-jobs-with-given-constraints/
 *
 * Approach:
 *
 * Complexity:
 *
 */

#include "iostream"

using namespace std;

int main() {
  int i = 0;
  int n = 0;
  // int *a = NULL; Use vector here
  cout << "Enter number of elements: ";
  cin >> n;
  a = (int *)malloc(sizeof(int) * n);
  for (i = 0; i < n; i++) {
    cout << "Enter element[" << i << "]: ";
    cin >> a[i];
  }
  return 0;
}
